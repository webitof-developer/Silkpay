# ==========================================
# BACKEND SERVER ENVIRONMENT CONFIGURATION
# ==========================================
#
# ⚠️  CRITICAL: Never commit this file with real credentials!
# Copy to .env for local development
#
# Generate secure keys:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# ==========================================

# ------------------------------------------
# SERVER CONFIGURATION
# ------------------------------------------

# Environment mode (development | production)
# Affects logging, error messages, and optimizations
NODE_ENV=development

# Server port
PORT=3001

# ------------------------------------------
# DATABASE
# ------------------------------------------

# MongoDB connection string
# Development: mongodb://localhost:27017/silkpay
# Production: mongodb+srv://user:pass@cluster.mongodb.net/silkpay
MONGODB_URI=mongodb://localhost:27017/silkpay

# ------------------------------------------
# AUTHENTICATION & SECURITY
# ------------------------------------------

# JWT secret key for token signing (⚠️  CHANGE IN PRODUCTION)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production-must-be-32-chars-minimum

# JWT token expiration time
# Examples: 15m, 30m, 1h, 7d
JWT_EXPIRY=30m

# Encryption key for sensitive data (account numbers, API keys)
# Must be 64 hex characters (32 bytes)
# ⚠️  CRITICAL: Generate new key for production, NEVER reuse dev keys
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# ------------------------------------------
# SILKPAY API INTEGRATION
# ------------------------------------------

# SilkPay API base URL
# Sandbox/Test: https://api.dev.silkpay.ai
# Production: https://api.silkpay.ai
# Backup: https://api.silkpay.help
SILKPAY_API_URL=https://api.dev.silkpay.ai

# Your SilkPay merchant ID (get from https://merchant.silkpay.ai/)
SILKPAY_MERCHANT_ID=your-merchant-id

# Your SilkPay secret key (get from Merchant Console)
# ⚠️  Keep this secret! Used for request signing
SILKPAY_SECRET_KEY=your-secret-key

# ------------------------------------------
# CORS & FRONTEND
# ------------------------------------------

# Frontend application URL
FRONTEND_URL=http://localhost:3000

# Backend URL
BACKEND_URL=http://localhost:3001

# Allowed CORS origins (comma-separated for multiple)
# Development: http://localhost:3000
# Production: https://app.yourdomain.com,https://admin.yourdomain.com
CORS_ORIGINS=http://localhost:3000

# ------------------------------------------
# EMAIL CONFIGURATION (SMTP)
# ------------------------------------------

# SMTP server settings for sending emails (password reset, notifications)
# Gmail example shown - configure your email provider
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Your email credentials
# ⚠️  For Gmail: Use App Password, not account password
# Generate at: https://myaccount.google.com/apppasswords
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# "From" address for outgoing emails
EMAIL_FROM=noreply@silkpay.local

# ------------------------------------------
# LOGGING
# ------------------------------------------

# Log level (debug | info | warn | error)
# Development: debug (verbose logging)
# Production: info or warn (less verbose)
LOG_LEVEL=debug

# ==========================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ==========================================
# Before deploying to production, ensure:
# 
# [ ] NODE_ENV=production
# [ ] Generate new JWT_SECRET (32+ characters)
# [ ] Generate new ENCRYPTION_KEY (64 hex chars)
# [ ] Update MONGODB_URI to production cluster
# [ ] Update SILKPAY_API_URL to https://api.silkpay.ai
# [ ] Use production SILKPAY_MERCHANT_ID
# [ ] Use production SILKPAY_SECRET_KEY
# [ ] Update CORS_ORIGINS to production domains
# [ ] Configure production SMTP credentials
# [ ] Set LOG_LEVEL to info or warn
# [ ] Never commit .env file to Git!
#
# See: server/PRODUCTION_CHECKLIST.md for complete guide
# ==========================================
